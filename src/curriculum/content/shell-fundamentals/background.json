{
  "concept": "job control",
  "course": "shell-fundamentals",
  "guide": {
    "concept": "job control",
    "description": "Appending & to a command runs it in the background and immediately hands the prompt back to you, instead of blocking until it finishes. The shell tracks it as a numbered job you can check on or bring back later.",
    "syntax": "cmd &",
    "flags": [{ "flag": "&", "desc": "run the preceding command in the background" }],
    "examples": [
      { "cmd": "sleep 5 &", "output": "[1] running in background", "note": "the prompt returns right away" }
    ]
  },
  "exercises": [
    {
      "id": "t2-background-1",
      "title": "Background a job",
      "task": "Start 'sleep 5' in the background.",
      "startFs": {
        "report.txt": { "content": "line one\nline two\nline three" },
        "report.csv": { "content": "id,amount\n1,100\n2,250" },
        "notes.md": { "content": "meeting notes" },
        "build.sh": { "content": "#!/bin/sh\necho building" }
      },
      "startCwd": [],
      "hints": ["Add & after the command to run it in the background", "sleep 5 is a safe command to background"],
      "criteria": [{ "id": "one-job", "label": "One job running in the background", "check": { "type": "state", "predicate": "bgJobCount", "min": 1 } }]
    },
    {
      "id": "t2-background-2",
      "title": "Background more than one job",
      "task": "Start two background jobs: 'sleep 3' and 'sleep 5'.",
      "startFs": {
        "report.txt": { "content": "line one\nline two\nline three" },
        "report.csv": { "content": "id,amount\n1,100\n2,250" },
        "notes.md": { "content": "meeting notes" },
        "build.sh": { "content": "#!/bin/sh\necho building" }
      },
      "startCwd": [],
      "hints": ["Each & starts its own separate job — run them one command at a time", "Any two sleep durations work, as long as they're two separate commands"],
      "criteria": [{ "id": "two-jobs", "label": "Two jobs running in the background", "check": { "type": "state", "predicate": "bgJobCount", "min": 2 } }]
    }
  ]
}
